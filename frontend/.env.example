# URL of the AXC1D FastAPI backend (web/backend).
# Optional - the app falls back to this exact value when unset.
# Copy to .env.local to override (e.g. a deployed API).
NEXT_PUBLIC_API_URL=http://localhost:8000
