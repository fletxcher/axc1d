[project]
name = "backend"
version = "0.1.0"
description = "AXC1D API - 1D meanline axial-compressor stage-stacking solver over HTTP"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "numpy>=2.5.2",
    "pandas>=3.0.5",
    "plotly>=7.0.0",
]
